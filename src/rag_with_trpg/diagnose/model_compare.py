from rag_with_trpg.diagnose.config import DiagnoseConfig


def compare(config: DiagnoseConfig):
    """2주차 Day 2 — 후보 4종의 dim / max_seq_length / 프리픽스 / 토크나이저 계열 비교.

    본문 미작성. 빈 본문은 SyntaxError 라 diagnose 패키지 전체가 import 되지 않으므로
    자리만 채운다. pass 가 아니라 raise 인 이유는 DO_MODEL_COMPARE=1 이 조용히
    아무 일도 안 하고 성공한 것처럼 끝나면 안 되기 때문이다 (D-21 침묵 버그).
    """
    raise NotImplementedError("model_compare: 2주차 Day 2 에서 작성한다")
